# مواصفة تكامل Ostora — المصدر الثالث (`os-`)
> مستودع `Ahmd3301/faselhd-db` | الإصدار 1.0 | 2026-09-18
> أمر صريح: **لا تعديل كودي الآن** — مواصفات فقط، والتنفيذ لاحقًا.
> المنهجية: قراءة الملفات البرمجية فقط (`*.py` + `*.cjs` + `*.yml`)، **وفحص حي فعلي** لـ API بفك التشفير أثناء كتابة هذه الوثيقة.

---

## 0) الهدف (كما طُلب حرفيًا)

1. كشط API الـ Ostora ليصبح `output/` ثلاثة مجلدات: `FaselHD` + `TopCinma` + **`Ostora`**.
2. داخل Ostora الأقسام العشرة المطلوبة (المفاتيح والـ IDs من `apixa.cjs` حرفيًا — البند 3.2).
3. تخزين **3 حقول فقط** لكل عمل: `id` + `name` + `image` (+ ميكانيكا الخط الموحدة).
4. **حل مشكلة الترتيب** رغم عدم الثقة بثبات ترتيب الـ API (البند 5.3 — الحل المصمم والمبرر).
5. الـ GitHub Action يكشط ويضع البيانات في Supabase (نفس الدفعات وحيدة الطلب).
6. `/start` بثلاثة أزرار، وقائمة Ostora الهرمية (رمضان / عربي / أفلام عربية ← 8 سنوات)، والبحث بمفاتيح `os-`.
7. رسالة التقرير بثلاث كتل باحترافية الشركات (البند 7).

---

## 1) الوضع الحالي الموثق (نقطة الانطلاق)

| المكوّن | الحالة |
|---|---|
| المصدران | FaselHD (8 مفاتيح `fd-`) + TopCinma (6 مفاتيح `tc-`) — المجموع 14 قسمًا في Supabase |
| السكربتات | `update.py` (`--root output/FaselHD`) + `topcinma_update.py` (`--root output/TopCinma`) — نفس عقد الدلتا والـ `ord` |
| الرفع | `supabase_push.py` يمسح المجلدين بدفعات 1000/طلب (`upsert on_conflict=section_key,slug`) |
| التقرير | `telegram_notify.py` — كتلتا FaselHD/TopCinma + `Total` (قالب `HTML`) |
| الـ workflow | `cron */30` + `timeout 25min`: update ← topcinma ← supabase_push ← notify |
| البوت | `/start` بزرين (`src:fd/src:tc`)، بحث غير حساس للحالة يشترط بادئة، بطاقة نصية لمن لا صورة له |

---

## 2) تحليل `apixa.cjs` (167 سطرًا — ليس كاشطًا بل **خادم وسيط**)

### 2.1 ما هو فعلًا

خادم Express دائم (`app.listen`) يفك تشفير استجابات تطبيق Ostora ويعيد نشرها عبر 10 endpoints محلية (`/os-ar-series` …). **لا يُستخدم كخادم في الـ Action** — بل يُنقل منطقه (الجلب + فك التشفير + ترقيم الصفحات) إلى سكربت دفعي `ostora_update.py`.

### 2.2 ثوابت البروتوكول (حرجة — تُنقل كما هي)

| الثابت | القيمة | ملاحظة |
|---|---|---|
| `BASE` | `https://mzqvyjnwmmx…sa036.shop/api/v6.2` | دومين مرآة — **قابل للتبدل** (كحالة دومينات TopCinma) ← قائمة مرشحين + fallback |
| `UA` | `ostora-5.5` | إلزامي في الترويسة |
| التشفير | AES-256-CBC + `key=4e5c6d…2b4e6f` + `iv=a9c21f…1a7b8e`، بدون `autoPadding` (تقشير PKCS7 يدويًا) | المفتاح مستخرج من التطبيق (ليس سرًا شخصيًا) لكن يُمركز في ثابت واحد ولا يُطبع أي رد خام في السجلات |
| الطلب | `GET {BASE}/category/{id}?page={n}` والرد نص base64 مشفر |
| الترقيم | حتى 50 صفحة، **التوقف عند `items.length < 400`** (حجم الصفحة 400)، `PAGE_DELAY=0` |

### 2.3 جدول الأقسام العشرة (IDs مطابقة لطلبك حرفيًا)

| المفتاح | ID | الاسم في الكود | تسمية القائمة |
|---|---|---|---|
| `os-ar-series` | 18 | مسلسلات عربية | مسلسلات عربي |
| `os-rn-series` | 31 | مسلسلات رمضان | مسلسلات رمضان |
| `os-arall-movies` | 334 | كل الأفلام العربية | افلام عربية |
| `os-ar26-movies` | 7038 | أفلام عربية 2026 | افلام عربية 26 |
| `os-ar25-movies` | 104 | أفلام عربية 2025 | افلام عربية 25 |
| `os-ar24-movies` | 4125 | أفلام عربية 2024 | افلام عربية 24 |
| `os-ar23-movies` | 3800 | أفلام عربية 2023 | افلام عربية 23 |
| `os-ar22-movies` | 3041 | أفلام عربية 2022 | افلام عربية 22 |
| `os-ar21-movies` | 1650 | أفلام عربية 2021 | افلام عربية 21 |
| `os-ar20-movies` | 414 | أفلام عربية 2020 | افلام عربية 20 |

### 2.4 حقول العناصر (مفاجأة موثقة: النوعان مختلفان)

- أقسام المسلسلات: `{id, name, image, adp, cat}` (مطابق `formatEpisode`).
- أقسام الأفلام: `{id, name, image, agent, ddx, download, player, source, type}` (حقول بث/تحميل — **تُتجاهل كلها**).
- المعتمد للتخزين: **`id` + `name` + `image` فقط** — أي حقل آخر ممنوع من ملفات JSON ومن قاعدة البيانات.

---

## 3) التحقق الحي (نُفّذ أثناء كتابة الوثيقة — بايثون + `cryptography`)

| الاختبار | النتيجة |
|---|---|
| `category/18?page=1` | ✅ فك التشفير ناجح — **400 عنصر** (صفحة ممتلئة ← توجد صفحات تالية)، `first_id=7662`، `has_image=True` |
| `category/104?page=1` | ✅ **47 عنصرًا** (< 400 ← صفحة واحدة فقط)، `first_id=154301`، `has_image=True` |
| الاستنتاج | البروتوكول سليم end-to-end؛ الحركة المتوقعة/تشغيل ضئيلة (معظم الأقسام 1–3 طلبات) |

---

## 4) شجرة `output/` الجديدة

```text
output/
  FaselHD/   (دون تغيير — 8 ملفات fd-*.json)
  TopCinma/  (دون تغيير — 6 ملفات tc-*.json)
  Ostora/    (جديد — 10 ملفات os-*.json + .delta/Ostora/)
```

⚠️ حساسية حالة الأحرف على Linux (كما في المواصفة السابقة): `Ostora` بهذا التهجئة في كل مرجع.

---

## 5) التصميم

### 5.1 شكل العنصر المخزن (العقد الموحد + الحقول الثلاثة)

```json
{ "section_key": "os-ar26-movies", "rank": 1, "slug": "154301",
  "name": "…", "link": "", "img": "https://…", "added_at": "2026-09-18T…Z" }
```

- `slug = str(id)` (مستقر عالميًا، والمفتاح المركب يعزل الأقسام).
- `link = ""` (الـ API بلا روابط ويب — لا روابط مختلقة). عمود Supabase يقبل `""`، و**البوت يخفي زر الرابط عند فراغه** (تعديل سطرين في `buildItemView`).
- `img ← image` (بفلتر `cover.jpg` الاحترازي الموحد).

### 5.2 `ostora_update.py` (المواصفة الدقيقة)

```text
OS_SECTIONS = [(key, wp_id) × 10]   # من جدول 2.3
BASE_CANDIDATES = [sa036.shop…, …]  # fallback بالترتيب عند فشل فك التشفير/الاتصال
```

- فك التشفير: `cryptography` (`AES/CBC` + تقشير PKCS7 يدويًا — نقل حرفي لـ `decryptAES`)؛ أي رد يفشل فكه = يُعامل كفشل شبكة (retry) لا كصفحة فارغة.
- الاعتمادية الجديدة الوحيدة: `cryptography` في `requirements.txt` (مكتبة ناضجة، wheels لكل المنصات).
- الأدب الشبكي: `sleep 1s` بين الصفحات (مقابل 0 في المرجع — الـ Action دوري لا يحتاج السرعة)، `retries=5` بتراجع أسّي، سقف 50 صفحة/قسم.
- نفس CLI (`--section/--dry-run/--root/--full`) + `SKIP_GITHUB_PUSH` + كتلة `FAILED` + سجل v2 بوسم `[ostora]`.

### 5.3 حل الترتيب (إجابة سؤالك: API قد يعيد الترتيب — التصميم محصّن)

**المبدأ: القرار بالانتماء للمجموعة (set-membership) لا بالموضع.**

1. كل تشغيل يجلب **القائمة الكاملة** للقسم (رخيص: 400/صفحة — القسم الأكبر ≈ 2–4 طلبات).
2. `new = fetched_ids − stored_ids` — الجديد فقط يُزاح للمقدمة (`rank` جديد + `added_at=الآن` + `ord` تحت `MIN(ord)` — نفس عقد fd/tc).
3. **العناصر الموجودة تحافظ على ترتيبها المخزن ولا يُعاد ترتيبها لترتيب الـ API أبدًا** — أي إعادة ترتيب من المصدر تصبح عديمة الأثر تلقائيًا.
4. تُحسب **درجة الانحراف** (نسبة العناصر التي غيّرت موضعها النسبي) وتُسجل في السطر — للمراقبة فقط دون أي فعل (`reorder_drift: 12%`).
5. المحذوف من المصدر **يبقى** (نفس سياسة المصدرين: لا حذف) — فيظهر في الانحراف لاحقًا إن عاد.

بهذا: لو ثبّت الـ API ترتيبه استفدنا، ولو بعثره لم نخسر شيئًا — والبوت (`ORDER BY ord ASC`) لا يتأثر إطلاقًا.

### 5.4 Supabase و`supabase_push.py`

- الجذر الثالث `output/Ostora` (كامل + دلتا) — **صفر تغيير في السكيمة** (10 صفوف `INSERT` في `sections` فقط).
- `to_row()` يقبل `link=""` و`img` الحاضر — دون تعديل (القيم الفارغة مدعومة أصلًا).

### 5.5 الـ workflow

```yaml
timeout-minutes: 30            # بدل 25 (ثلاثة مصادر)
steps:
  - python update.py            # FaselHD (ثابت)
  - python topcinma_update.py   # TopCinma (ثابت)
  - python ostora_update.py     # Ostora (جديد — نفس الأسرار GIT_*)
  - supabase_push.py + telegram_notify.py  # (ثابتان)
```

بلا أسرار جديدة. الحركة الشبكية الإضافية ≈ 15–25 طلبًا/تشغيل (تافهة).

### 5.6 الاختبارات (المطلوبة مع التنفيذ)

- `test_os_decrypt`: تشفير عينة بـ AES ثم فكها (round-trip بدون شبكة).
- `test_os_setdiff`: ترتيب API مبعثر عمدًا (جديد في المنتصف + محذوف) ← الجديد فقط في المقدمة والقديم بترتيبه.
- `test_os_log_block`: كتلة `[ostora]` تُقرأ في التقرير الثلاثي.

---

## 6) البوت — هرمية Ostora (العرض الحرفي)

### 6.1 `/start` — ثلاثة أزرار

```
┌──────────────┬──────────────┬──────────────┐
│ 🎬 FaselHD   │ 🎞 TopCinma  │ 🌙 Ostora    │
└──────────────┴──────────────┴──────────────┘
```
`callback_data`: `src:fd` / `src:tc` / `src:os` (الموجود + واحد).

### 6.2 `src:os` — مجموعات Ostora الثلاث

```
┌──────────────────────┬──────────────────────┐
│ 🌙 مسلسلات رمضان     │ 📺 مسلسلات عربي      │
├─────────────────────────────────────────────┤
│            🎬 أفلام عربية                    │
├─────────────────────────────────────────────┤
│               🔙 المصادر                     │
└─────────────────────────────────────────────┘
```

- `مسلسلات رمضان` ← يفتح `os-rn-series` **مباشرة** (طفل وحيد — لا نقرة زائدة).
- `مسلسلات عربي` ← يفتح `os-ar-series` **مباشرة**.
- `أفلام عربية` ← قائمة فرعية (`osg:movies`):

```
┌──────────────────────┬──────────────────────┐
│ افلام عربية          │ افلام عربية 26       │
│ افلام عربية 25       │ افلام عربية 24       │
│ افلام عربية 23       │ افلام عربية 22       │
│ افلام عربية 21       │ افلام عربية 20       │
├─────────────────────────────────────────────┤
│ 🔙 Ostora             │ 🔙 المصادر           │
└─────────────────────────────────────────────┘
```

سلسلة الرجوع: قسم ← `osg:movies` ← مجموعات `src:os` ← مصادر `src:back` (تُحمل في `backData` الحالية دون توسيع حد 64 بايت: أطول مفتاح `os-arall-movies` = 14 حرفًا).

### 6.3 البحث — مفاتيح `os-` مثل أخواتها

```text
/search os-ar-series <اسم>     → مسلسلات عربي فقط (مثال: /search os-ar-series الحشاشين)
/search os-ar26-movies <اسم>   → أفلام 2026 فقط
```

غير حساس للحالة، ويشترط بادئة (`fd-`/`tc-`/`os-`)، وخطأ المفتاح يعرض الـ 24 مفتاحًا مجمعة بالمصدر.

### 6.4 تعديلان صغيران ملزمان

1. `buildItemView`: إخفاء زر `🔗 الرابط` عندما `link` فارغ (كل عناصر `os-`) — تبقى البطاقة (صورة إن وجدت + رجوع).
2. `/stats`: كتلة ثالثة `🌙 Ostora` (10 أسطر).

---

## 7) الرسالة المرسلة — القالب الثلاثي (HTML)

### 7.1 القواعد (امتداد v2: مصدر ثالث، نفس الانضباط)

رقم واحد/مصدر + إجمالي واحد؛ المحدّثة فقط في `<code>` تنازليًا؛ الأصفار في `skipped:`؛ `✅ up to date` بلا كتلة؛ `🔴 failed` + `⚠ partial`؛ ≈ 900 حرف « 4096.

### 7.2 القالب الرسمي

```html
<b>🔄 Auto Update Report #2175</b>
🗓 Thursday, September 18, 2026, 02:46:56 PM
<b>━━━━━━━━━━━━━━</b>
🎬 <b>FaselHD</b> — 🆕 +10 new · ⏱ 48s
<code>fd-series        +4 new
fd-movies        +2 new
fd-asian-series  +2 new
fd-anime         +1 new
fd-tvshows       +1 new</code>
<i>skipped: fd-asian-movies, fd-hindi, fd-anime-movies</i>
<b>━━━━━━━━━━━━━━</b>
🎞 <b>TopCinma</b> — 🆕 +6 new · ⏱ 41s
<code>tc-movies-foreign  +3 new
tc-series-anime    +2 new
tc-anime-movies    +1 new</code>
<i>skipped: tc-movies-asian, tc-series-foreign, tc-series-asian</i>
<b>━━━━━━━━━━━━━━</b>
🌙 <b>Ostora</b> — 🆕 +5 new · ⏱ 12s
<code>os-rn-series     +3 new
os-ar26-movies   +2 new</code>
<i>skipped: os-ar-series, os-arall-movies, os-ar25-movies, os-ar24-movies, os-ar23-movies, os-ar22-movies, os-ar21-movies, os-ar20-movies</i>
<b>━━━━━━━━━━━━━━</b>
📦 <b>Total: +21 new</b> · FaselHD 10 + TopCinma 6 + Ostora 5 · ⏱ 101s
⏳ Next report in 30 minutes.
```

### 7.3 صيغة `update_log.txt` v3 (كتلة ثالثة + إجمالي ثلاثي)

```text
Run: …Z [ostora]
[os-rn-series      ] +3 -> new total
[os-ar-series      ] +0 -> skipped
… (الأقسام العشرة) …
Total new (ostora): 5 items across 2 sections
Duration (ostora): 12s
GRAND TOTAL: 21 new items | Faselhd 10 (48s) + TopCinma 6 (41s) + Ostora 5 (12s)
```

`telegram_notify.py` يقرأ الكتل الثلاث (الأقدم تُتجاهل) ويحسب الإجمالي — متوافق رجعيًا مع الكتل الحالية.

---

## 8) Checklist التنفيذ (لاحقًا — مرتب صارم)

- [ ] **P0**: SQL: `INSERT` صفوف `sections` العشرة (نفس ملف السكيمة + `upsert` REST).
- [ ] **P0**: `ostora_update.py` (5.2 + حل الترتيب 5.3) + `cryptography` في `requirements.txt` + مجلد `output/Ostora/`.
- [ ] **P0**: أول كشط كامل (10 أقسام — دقائق معدودة) + `supabase_push.py --full` للجذر الثالث + مطابقة الأعداد والترتيب.
- [ ] **P1**: `telegram_notify.py` (الكتلة الثالثة + الإجمالي الثلاثي) + workflow (`timeout 30` + الخطوة).
- [ ] **P1**: البوت (زر `src:os` + المجموعات + `osg:movies` + إخفاء زر الرابط الفارغ + `/stats`).
- [ ] **P2**: الاختبارات الثلاثة (5.6) + `workflow_dispatch` تجريبي ومقارنة العدد مع `count` الفعلي.
- [ ] **P2**: تدوير أي أسرار لامست `apixa.cjs` إن وجدت، وتوثيق دومين `BASE` كمرشح قابل للتبدل.

**القبول:** أعداد Supabase = `total` كل JSON (24 قسمًا)؛ `ord` تصاعدي والأحدث أولًا؛ اختبار بعثرة الترتيب يمر؛ الرسالة تطابق 7.2 في الحالات الثلاث؛ فشل مصدر لا يكسر الآخرين.

**التراجع:** إيقاف خطوة Ostora + `git revert` — المصدران الآخران مستقلان تمامًا.

---

## 9) الملاحظات الأمنية والتشغيلية

1. مفتاح/IV الـ AES **مضمنان في `apixa.cjs`** (مستخرجان من التطبيق لا سران شخصيان) — يُنقلان لثابت مركزي واحد، ويُمنع طباعة أي رد خام أو مفتاح في سجلات CI.
2. دومين `BASE` (`…sa036.shop`) بنية مرايا دوّارة على الأرجح — التصميم يدعم قائمة `BASE_CANDIDATES` من اليوم الأول.
3. عناصر الأفلام تحمل حقول بث (`player/download/source`) — **تُهمل عمدًا** (نطاق الكشط: فهرسة فقط).

---

## 10) المراجع المعتمدة

1. `apixa.cjs` (محلي): الأسطر 8–11 (BASE/UA/AES)، 14–25 (الأقسام العشرة)، 64–102 (الجلب والترقيم وحجم 400)، 64–70 (حقول العنصر).
2. فحص حي 2026-09-18: `category/18` (400 عنصر/صفحة) و`category/104` (47 عنصرًا) مع فك تشفير ناجح.
3. ملفات المستودع الحالية: `update.py` / `topcinma_update.py` / `supabase_push.py` / `telegram_notify.py` / `update.yml` (بنية المجلدين + عقد `ord` + قالب v2).

---

*نهاية الوثيقة — جاهزة للتنفيذ المباشر دون أي تعديل على الكود الحالي.*
